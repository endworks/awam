import { UpcomingEventsPipe } from './upcoming-events.pipe';

describe('UpcomingEventsPipe', () => {
  it('create an instance', () => {
    const pipe = new UpcomingEventsPipe();
    expect(pipe).toBeTruthy();
  });
});
